name: stackramp-example

frontend:
  framework: react
  dir: frontend
  node_version: "20"

backend:
  language: python
  dir: backend
  port: 8080

domain: stackramp.io

quality:
  fail_on_gate: false  # docs site — complexity heuristics misfire on static JSX content pages
